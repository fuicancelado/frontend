import styled from 'styled-components';

export const SearchBtn = styled.button`
    border: none;
    background: var(--green-400);
    color: var(--gray-200);
    border-radius: 5px;
    padding: .5rem 1rem;

    display: flex;
    align-items: center;

    svg { 
        margin-right: .5rem;
    }
`;