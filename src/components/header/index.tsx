import React from 'react';
import { Container } from '../../pages/Error/styles';

const Header: React.FC = () => {
    return (
        <Container>
            hello word
        </Container>
    );
};

export default Header;